"""Instruction templates for tool integrations.

Templates are functions that take parameters (tool name, python path) and
return instruction text. Separated from injection logic for testability.
"""
